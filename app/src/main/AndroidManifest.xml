<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rjl77.spacetrader">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".views.PlanetScreen"
            android:theme="@style/AppTheme.NoActionBar.FullScreen"
            android:noHistory="true"></activity>
        <activity android:name=".views.BuySell"
            android:theme="@style/AppTheme.NoActionBar.FullScreen"
            android:noHistory="true"></activity>
        <activity android:name=".views.MarketView"
            android:theme="@style/AppTheme.NoActionBar.FullScreen"
            android:noHistory="true"></activity>
        <activity android:name=".views.Travel"
            android:theme="@style/AppTheme.NoActionBar.FullScreen"
            android:noHistory="true"></activity>
        <activity android:name=".views.CreateActivity"
            android:theme="@style/AppTheme.NoActionBar.FullScreen"
            android:noHistory="true"></activity>
        <activity android:name=".views.MainScreen"
            android:theme="@style/AppTheme.NoActionBar.FullScreen"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>